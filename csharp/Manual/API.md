# API Overview

`MarcusMedina.Decision` exposes its functionality through the public types in the `csharp/src` project.

## Main Public Surface

- Builders and fluent entry points in the package root namespaces
- Core value objects and domain types under the package's `Core` folders
- Extension methods where the package exposes fluent helpers

## Usage Guidance

Read the source under `csharp/src` together with unit tests under `csharp/tests` to understand the current API behavior while the package is still in `_WIP_`.
